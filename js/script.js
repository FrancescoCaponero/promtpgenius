(function($) {
    // Il tuo codice JavaScript va qui
    // Puoi usare $ normalmente all'interno di questa funzione

    $(document).ready(function() {
        // Esempio: cambio colore di un elemento
        $(".my-element").css("color", "blue");
    });

})(jQuery);
